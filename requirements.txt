streamlit
anthropic
pdfplumber
python-docx
# Mistral (preproducción) NO necesita paquete: se llama a su API por HTTP con
# httpx, que ya viene instalado como dependencia del SDK de Anthropic. El
# paquete `mistralai` se retiró de aquí porque sus dependencias no siempre
# tienen versión compatible con el Python del entorno de despliegue.
